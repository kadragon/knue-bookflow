# Backlog

## Now

(none)

## Next

- [ ] Cron observability: surface last-run timestamp + success/failure for the `0 3 * * *` pipeline (renewal / due-soon broadcast / request-book sync) somewhere queryable — currently only inspectable via Wrangler logs.
- [ ] Telegram correction loop: document the reply-to-edit / reaction flow in `docs/` (spans `services/note-broadcast.ts` + `handlers/telegram-webhook-handler.ts`) so future edits don't need to reverse-engineer it.

## Someday
