spec_id: SPEC-bugfix-001
title: "Fix Undefined Display in New Books List"
given_when_then:
  - given: "The New Books page is loaded"
    when: "The book list is rendered"
    then: "Each book entry should display the correct title, author, and publisher, without 'undefined(undefined)' text."
acceptance_tests:
  - id: TEST-bugfix-001
    desc: "Verify that the New Books list renders title and author correctly."
dependencies:
  - governance: "default"
linked_tasks:
  - TASK-046
