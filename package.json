{
  "name": "knue-bookflow-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.2.20",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/backend dev",
    "dev:frontend": "bun run --cwd packages/frontend dev",
    "build:frontend": "bun run --cwd packages/frontend build",
    "build:backend": "bun run --cwd packages/backend build",
    "deploy": "bun run --cwd packages/backend deploy",
    "db:migrate": "bun run --cwd packages/backend db:migrate",
    "build": "bun run --cwd packages/shared build && bun run --cwd packages/frontend build && bun run --cwd packages/backend build",
    "test": "vitest --run --configLoader runner",
    "lint": "biome check .",
    "typecheck": "tsc --build",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "prepare": "simple-git-hooks"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,json,css,mts,cts,mjs,cjs}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  },
  "simple-git-hooks": {
    "pre-commit": "bunx lint-staged && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.12",
    "lint-staged": "^16.2.7",
    "simple-git-hooks": "^2.13.1",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
