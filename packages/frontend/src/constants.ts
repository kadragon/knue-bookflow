/**
 * Shared layout constants for consistent spacing across the application
 */

/**
 * Bottom padding for page containers to accommodate the fixed bottom navigation
 */
export const PAGE_CONTAINER_PADDING_BOTTOM = 10;
