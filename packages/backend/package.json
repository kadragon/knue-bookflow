{
  "name": "@knue-bookflow/backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "test": "vitest run",
    "cf-typegen": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply knue-bookflow-db --remote"
  },
  "dependencies": {
    "@knue-bookflow/shared": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.15",
    "@cloudflare/workers-types": "^4.20251205.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.15",
    "wrangler": "^4.53.0"
  }
}
