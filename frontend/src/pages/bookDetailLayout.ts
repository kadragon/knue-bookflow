import type { SxProps, Theme } from '@mui/material/styles';

// Trace: spec_id: SPEC-book-detail-001, task_id: TASK-031

/**
 * Shared layout styles for the book detail page.
 *
 * Notes list should flow with the page height instead of creating an inner scrollbar.
 */
export const NOTES_LIST_SX: SxProps<Theme> = {};
